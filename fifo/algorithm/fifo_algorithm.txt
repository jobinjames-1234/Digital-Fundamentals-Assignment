Step 1: Start
Step 2: Declare an integer array queue[size] and initialize front = -1 and rear = -1
Step 3: Display menu with options: Enqueue, Dequeue, Display, Exit
Step 4: Read the user’s choice
Step 5: If the choice is Enqueue, check if rear == size - 1; if true, display Queue Overflow; otherwise read the element, if front == -1 set front = 0, increment rear and insert the element into queue[rear]
Step 6: If the choice is Dequeue, check if front == -1 or front > rear; if true, display Queue Underflow; otherwise delete the element at queue[front] and increment front
Step 7: If the choice is Display, check if front == -1 or front > rear; if true, display Queue is empty; otherwise display all elements from front to rear
Step 8: Repeat Steps 3 to 7 until the user selects Exit
Step 9: Stop